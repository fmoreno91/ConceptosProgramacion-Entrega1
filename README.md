# Project: Fundamental Concepts of Programming - Milestone 1

This repository contains the development for **Milestone 1 (Week 3)** of the Programming course. The primary objective is to generate pseudo-random data files that will serve as input for a sales processing system.

## 🚀 Key Features
The program developed in the `GenerateInfoFiles` class performs the following tasks:
* **Product Generation:** Creates a `productos.txt` file with random IDs, names, and unit prices.
* **Salesman Information:** Generates a `vendedores.txt` file linking names, last names, and ID documents.
* **Sales Simulation:** Creates individual files for each salesman (e.g., `ventas_ID.txt`) containing product records and quantities sold.

## 📂 Project Structure
* `src/`: Contains the Java source code (`GenerateInfoFiles.java`).
* `data/`: Folder where the `.txt` files generated by the program are stored.
* `.gitignore`: Configuration to avoid uploading unnecessary build files to the repository.

## 🛠️ Requirements & Setup
* **Language:** Java 8 or higher (Developed using JDK 26).
* **IDE:** VS Code / Eclipse.
* **Execution:** 1. Open the project in your IDE.
  2. Run the `GenerateInfoFiles.java` class.
  3. Verify the output files in the `data/` directory.

## 👤 Author
* **Andrés Felipe Moreno Suárez** - Software Engineering Student - Politécnico Grancolombiano.

========================================================================================================================================

# Proyecto: Conceptos Fundamentales de Programación - Entrega 1

Este repositorio contiene el desarrollo de la **Entrega 1 (Semana 3)** para la materia de Conceptos Fundamentales de Programación. El objetivo principal es la generación de archivos de datos pseudoaleatorios que servirán como entrada para un sistema de procesamiento de ventas.

## 🚀 Funcionalidades de esta entrega
El programa desarrollado en la clase `GenerateInfoFiles` realiza las siguientes tareas:
* **Generación de Productos:** Crea un archivo `productos.txt` con IDs, nombres y precios aleatorios.
* **Información de Vendedores:** Genera un archivo `vendedores.txt` vinculando nombres, apellidos y documentos de identidad.
* **Simulación de Ventas:** Crea archivos individuales para cada vendedor (ej. `ventas_ID.txt`) con registros de productos y cantidades vendidas.

## 📂 Estructura del Proyecto
* `src/`: Contiene el código fuente en Java (`GenerateInfoFiles.java`).
* `data/`: Carpeta donde se almacenan los archivos `.txt` generados por el programa.
* `.gitignore`: Configuración para evitar subir archivos basura al repositorio.

## 🛠️ Requisitos e Instalación
* **Lenguaje:** Java 8 o superior (Desarrollado con JDK 26).
* **IDE:** VS Code / Eclipse.
* **Ejecución:** 1. Abrir el proyecto en el IDE.
  2. Ejecutar la clase `GenerateInfoFiles.java`.
  3. Verificar los resultados en la carpeta `data/`.

## 👤 Autor
* **Andrés Felipe Moreno Suárez** - Estudiante de Ingeniería de Software - Politécnico Grancolombiano.